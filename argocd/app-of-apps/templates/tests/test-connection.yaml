apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-of-apps.fullname" . }}-test-connection"
  labels:
    {{- include "app-of-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-of-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
